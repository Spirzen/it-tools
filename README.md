# tools.spirzen.ru — инструменты «Вселенная IT»

Astro-портал (заготовка). Один repo = один домен (GitHub Pages).

| | |
|---|---|
| Прод | tools.spirzen.ru |
| Локально | http://localhost:4334 |

```bash
npm install
npm run dev
npm run build
```

Соседние порталы: `it-terms`, `it-lab`, `it-games`, `it-kids`. Общий nav: `ecosystem-urls.json`.
